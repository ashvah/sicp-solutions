#lang sicp

; in the formula r1r2/(r1+r2), the r1(or r2) in numerator and denominator are exactly the same number
; But our program cannot take them as the same
; so par2 is better since every variable appears only once
